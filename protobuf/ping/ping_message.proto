syntax = "proto3";
package ping;

// services

service Pinger {
    rpc GetPing (stream PingMessage) returns (stream PingMessage) {}
}

// messages

message PingMessage {
    string query = 1;
    int32 number = 2;
}