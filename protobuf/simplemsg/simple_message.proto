syntax = "proto3";
package simplemsg;

// services

service Greeter {
    rpc SayHello (PingMessage) returns (PingMessage) {}
}

// messages

message PingMessage {
    string query = 1;
    int32 number = 2;
}